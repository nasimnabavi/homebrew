build:
    tests:
        before:
            - gem install rubocop
        override: 
            - 
                command: 'rubocop'
                analysis:
                    file: 'data'
                    format: 'rubocop'
tools:
    rubocop: true

build:
    tests:
        before:
            - gem install rubocop
        override: 
            - 
                command: 'rubocop --format json'
                analysis:
                    file: 'rubocop-data'
                    format: 'rubocop-json'


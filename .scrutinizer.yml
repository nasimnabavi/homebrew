build:
    tests:
        before:
            - gem install rubocop
        override: 
            - 
                command: 'rubocop -o data'
                analysis:
                    file: 'rubocop-data'
                    format: 'rubocop'


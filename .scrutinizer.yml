build:
    tests:
        before:
            - gem install rubocop
        override: 
            - 
                command: 'rubocop --format json -o rubocop-data'
                analysis:
                    file: 'rubocop-data'
                    format: 'rubocop'


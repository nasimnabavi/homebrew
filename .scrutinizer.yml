build:
    tests:
        before:
            - gem install rubocop
        override: 
            - 
                command: 'rubocop -o rubocop-data --format json'
                analysis:
                    file: 'rubocop-data'
                    format: 'rubocop'

